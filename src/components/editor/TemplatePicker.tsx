import type { Template } from "../../types/models"; import { templateLabels } from "../../utils/templates"; export function TemplatePicker({value,onChange}:{value:Template;onChange:(v:Template)=>void}){return <select value={value} onChange={e=>onChange(e.target.value as Template)} className="rounded-xl border bg-transparent px-3 py-2 text-sm">{Object.entries(templateLabels).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select>;}